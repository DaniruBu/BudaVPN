---
- name: Set system hostname
  hostname: 
    nama: "{{ server_hostname }}"

- name: Map hostname in /etc/hosts
  ansible.builtin.lineinfile:
    path: /etc/hosts
    regexp: '^127\.0\.1\.1\s+'
    line: "127.0.0.1 {{ server_hostname }}"
    state: present